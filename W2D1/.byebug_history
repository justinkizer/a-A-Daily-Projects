exit
end
square = "X"
          binding.pry
        row.each do |square|
      if [0,1,@grid.length-1, @grid.length-2].include?(i)
    @grid.each_with_index do |row, i|
  def setup
  end
    @grid = Array.new(8) { Array.new(8) { NullPiece.new} }
  def initialize
class Board
